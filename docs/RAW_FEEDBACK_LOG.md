# Raw Feedback Log

## Jan 29, 2026

"First element is the challenges, that's one bucket where we need to make sure that the challenges get removed and marked as not done by the end of the day. So every day you get a new set of challenges and the old ones disappear. By default, one challenge per day, but that can be changed in the settings. Additionally for the challenges, when creating a new challenge, it does not appear underneath the goal. So it doesn't get displayed as nested underneath the goal after creation. It needs to be. That is a bug.

The habit voice log, so when trying to track habits with the voice log, that one still does not open up over the habit page. It doesn't open a new page above it. It still opens another section below.

The voice recorder or yeah, in general the voice entry um works well but I cannot press the pulsing microphone icon to finish as well. That should be added.

The tracking drop down has been corrected in the top bar, but it is still broken in the bottom navigation bar. So make sure that you um take the functionality um from the top bar and add it to the bottom bar. Additionally, I would like the style to be a little bit nicer. Space the drop-down more, create like pills maybe for each for each item and space it more, make the pop-up a little bit bigger. Yeah.

The progress page needs a bit of an update as well. The last 30 days calendar view is extremely overwhelming. Like it's very hard to see and understand what's going on. So we need a redesign. I am thinking we keep the general idea of having a like a hist like having markers for completed, skipped, pending and no challenge. But for the actual goals, one goal is pre-chosen, pre-selected, and then you can click it and you have a drop-down of all the other goals. So you can select for every goal how this looks. Mood and energy levels trends, that one is pretty good direction-wise. But I would like to implement something similar. Select the first goal, select mood maybe by default, and then you have options to pick energy and motivation as well. And if you and that also means that you only display one thing, which means we can display a nice curve displaying the mood levels over time and the energy and motivation levels.

In the progress page, please remove the navigation at the bottom for daily check-in and home.

I'm happy to say that expert chat is successfully fixed. We fill the entire screen and we're no longer cutting off some stuff to the right. That unfortunately is not the case for the voice-based check-in. So if we click check-in and then head over to talk to AI and the guided interview that one is still that one is still weird. Take the expert chat and port those fixes over to the to the voice-based interview. Check-in. The only thing we need to replace, obviously, is the progress bar and the different indicators at the top. That means instead of having the choice of which expert to talk to, you now see the progress throughout the different phases of the check-in.

Then we have a general problem that also manifests in the voice-based check-in and that is it seems like all of the AI yeah like the AI experts but then also the check-in do not have knowledge about other goals except for one of them. In the demo account, which I'm currently working in, it is only the marathon training that is getting picked up. Any other goal can't be seen. And in the AI-based check-in, that also means that only the check-in only asks questions about that specific goal. That should not be the case. In general, all of the AI should have full knowledge about all goals and all challenges. So that is a bug that needs fixing. And the AI-based check-in should ask about every single goal that the user has before moving on to the next item.

And then I have a big bucket which is the chat interface and the functionality, like the voice-based chat interface and functionality. Currently what happens once you type a test message is it takes a little bit of time and then it stays a little bit at the top like I want, right? It doesn't scroll while it's generating. But then once it's finished generating the answer, it scrolls all the way to the bottom. Or towards the bottom. I'm not sure if it if it scrolls all the way to the bottom. Let's see. And it automatically goes into it opens the keyboard up again, which also I do not want. And yeah, it does scroll all the way to the bottom. So that needs to be fixed. When sending a message, keyboard should be hiding again, should hide again. It does already. Then the message should generate and it should stay at the very top. The user has to be in control of scrolling. The system should never scroll. And then at the end, it should not open up the keyboard again.

And then one thing about the the voice chat, um like design wise, I would like more space for the text, so please hide the icons to the left and instead um and also for the user, don't don't show the user icon, just keep the color as you currently have. And um because you're hiding the icon of the coach to the left, um use that space to completely fill it with text. Also there's a little bit of space to the left, to the right and to the top that we can fill with this window, um with this chat window. So use that space as well and um yeah, to really maximize the amount of space that text can show. So the user has to scroll, less.

And then I have one more which is a bigger thing. I would like to be able to directly talk to the eye. So live talking. Where we need a new button which allows the user to or which opens up a new window which is the live chat where whatever voice mode they're currently in so this would need to be for both the voice-based check-in and also the expert chat whatever mode they're in still gets done like still gets executed they're still talking to the same coach they're still doing the check-in but all they can see is this like floating like pulsing icon and they can talk and then once they're done talking give a little bit of a grace period I think four seconds should be good and then the AI should answer ideally and the goal is to have the AI answer as fast as possible so there's no real delay and then yeah keep talking maybe we need to implement some live streaming for that I'm not sure maybe we can structure the answer into three parts or something let's try and be creative there and try and get to a place that feels pretty much like a real conversation with very little delay

Lastly, I would like to have different voice options in the settings. At least different voices the user can choose from for all of the voice-based functionalities of the app. Ideally, maybe even some speed of the voice. If it's possible, maybe some like some some verbal functionality around how excited they talk, how sad they talk, that kind of stuff."

## Jan 28, 2026

"Most importantly, when creating a new challenge or new challenges underneath a goal, like nested under a goal, connected to a goal, it creates them, I think, but they do not show up underneath the goal visually. So at the dashboard, after creating new challenges the challenges do not show up, the new ones. Old ones seem to do, but new ones do not show up.
(...)
